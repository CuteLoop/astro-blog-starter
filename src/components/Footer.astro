---
import Social from "./Social.astro";

const profiles = [
  { platform: "GitHub", username: "cuteloop" },
  { platform: "LinkedIn", username: "joel-maldonado-9ab79a15b" },
];
---

<footer class="site-footer">
  <div class="wrap">
    <p class="meta">Built with Astro. Customize <code>src/config/site.ts</code> to make it yours.</p>

    <ul class="footer-links">
      {profiles.map(({ platform, username }) => (
        <li><Social platform={platform} username={username} /></li>
      ))}
    </ul>

    <p class="meta">© {new Date().getFullYear()} Cosmic Paper</p>
  </div>
</footer>

<style>
  .footer-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    list-style: none;
    padding: 0;
    margin: 0.75rem 0 1.25rem;
  }

  code {
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", monospace;
    font-size: 0.95em;
    padding: 0.1rem 0.35rem;
    border-radius: 0.4rem;
    border: 1px solid rgba(255,255,255,0.18);
    background: rgba(255,255,255,0.06);
    color: rgba(255,255,255,0.92);
  }
</style>
